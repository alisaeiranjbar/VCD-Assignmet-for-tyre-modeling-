# VCD-Assignmet-for-tyre-modeling-
The script simulates and plots brake force (Fx) and side force (Fy) over longitudinal slip (κ from 0% to 100%), with slip angle (α) as a parameter. It assumes equal weight distribution over four wheels and uses SciPy constants for gravity.
